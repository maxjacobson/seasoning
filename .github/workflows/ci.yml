name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  RAILS_ENV: test
  CI: 1
  COVERAGE: 1
  SEASONING_DATABASE_USERNAME: postgres
  SEASONING_DATABASE_PASSWORD: postgres
  RUBYOPT: "--enable=frozen-string-literal --debug=frozen-string-literal"

jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: seasoning_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v7.0.0
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1.314.0
        with:
          ruby-version: 4.0.5
          bundler-cache: true
      - name: Use Node.js
        uses: actions/setup-node@v6.4.0
        with:
          node-version-file: .node-version
          cache: npm
      - run: npm install -g npm && corepack enable npm
      - run: npm clean-install
      - run: npm run playwright:install
      - run: bin/lint
      - run: bundle exec rake assets:precompile
      - run: bin/rails db:create db:schema:load
      - run: bin/rails test:all
      - run: bin/assert-no-pending-allow-scripts

      - name: Upload capybara screenshots
        uses: actions/upload-artifact@v7
        with:
          name: capybara-screenshots
          path: tmp/screenshots/
          retention-days: 7
        if: ${{ failure() }}
