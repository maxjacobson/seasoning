#!/bin/sh

set -e

path="tmp/digests-Gemfile.lock.txt"

if test -f $path; then
  if md5sum --status --strict --check "$path"; then
    exit 0
  fi
fi

bundle install --quiet
md5sum Gemfile.lock > "$path"
