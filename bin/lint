#!/bin/sh

set -e

echo "Reminder: if this script fails, you can run bin/fix-lints to fix most issues"

PATH="node_modules/.bin:$PATH"

set -x

prettier --check .
herb-format --check .
herb-lint .
bin/rubocop
bin/erb_lint --lint-all --show-linter-names
