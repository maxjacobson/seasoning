<%# locals: () %>

<%= form_with url: "/search", class: "md:mr-2 md:inline", method: :get do |f| %>
  <%= f.text_field :q, placeholder: "Search", class: "text-field mr-2" %>
  <%= f.submit "Search", class: "rounded-button" %>
<% end %>
