<%= content_for :title do %>
  <%= @profile.handle %>&rsquo;s followers
<% end %>

<% if @profile.followers.any? %>
  <h1 class="text-2xl"><%= @profile.handle %>&rsquo;s followers</h1>
  <ol class="list-inside list-decimal">
    <% @profile.followers.each do |human| %>
      <li>
        <%= link_to human.handle, human_profile_path(human.handle) %>
      </li>
    <% end %>
  </ol>
<% else %>
  <h1 class="text-2xl"><%= @profile.handle %>&rsquo;s followers</h1>
  <p>None yet!</p>
<% end %>
